spring:
  application:
    name: SIMULENEM-backend
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:h2:mem:simulenem
    username: sa
    password: password
  h2:
    console:
      enabled: true
